# Request your CMS Marketplace API key at:
# https://developer.cms.gov/marketplace-api/key-request.html
CMS_API_KEY=d687412e7b53146b2631dc01974ad0a4

# Node environment (development | production)
NODE_ENV=development

# Set to true in staging or preview environments where NODE_ENV is not 'production'
# but TLS is active. In production, NODE_ENV=production activates the secure flag
# automatically. Omitting this in non-production HTTPS deployments will transmit
# the session cookie over HTTP.
# COOKIE_SECURE=true
